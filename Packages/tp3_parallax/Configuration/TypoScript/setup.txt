


lib.parallax = FILES
lib.parallax {
    references {
        table = pages
        uid.data = tsfe:id
        fieldName = page_parallax
    }

    begin = 0
    maxItems = 5

    renderObj = IMG_RESOURCE
    renderObj {

        5.wrap = var section_image = {"section#p|":
        10{
            import.current = 1
            treatIdAsReference = 1
            # todo get size from register
            #                // beispiel breite höhe
            width = 1920
            maxH = 1080
            file.import.dataWrap = {file:current:storage}:{file:current:identifier}
            altText.data = file:current:title

        }
    }
    stdWrap.wrap = <script> var section_image;|</script>
}

wrap = <script>|</script>
5 = TEXT
5.data = page:uid
5.wrap = var section_image = {"section#p|":
10 = TEXT
10.wrap = [|]}
10.data = levelmedia:-1,slide
10.override.if.isFalse.field = media
10.split {
    token = ,
    cObjNum = 1
    max = 10
    sort = rand()

    1 {

        10 = IMG_RESOURCE
        #  params = class="flexible"
        10.file {
            import.current = 1
            import = uploads/media/
            treatIdAsReference = 1
            # todo get size from register
            #                // beispiel breite höhe
            width = 1920
            maxH = 1080
        }
    }

    1.wrap = {background:"|"},
}

page.headerData {

    112113 >
        112113 < lib.parallax
}